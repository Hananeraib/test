image: docker:20.10.16
variables:
  DOCKER_TLS_CERTDIR: ""
services:
  - docker:20.10.16-dind

default:
  tags:
    - docker


stages:
  - build

build:
  stage: build
  before_script:
  - docker login -u admin -p "JFrog@dmin2023" vps-da62cdaf.vps.ovh.net:8082/artifactory/pfeblog/
  script:
    - docker compose build
    - docker compose push

# down_compose:
#   stage: .post
#   script:
#     - docker images
#     - docker ps
#     - docker compose down
#   when: manual
